<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MFA Setup Complete</title>
    <style>
        :root {
            --primary: #4f46e5;
            --success: #10b981;
            --gray-50: #f9fafb;
            --gray-100: #f3f4f6;
            --gray-500: #6b7280;
            --gray-700: #374151;
            --gray-900: #111827;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 1rem;
        }

        .container {
            background: white;
            border-radius: 1rem;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
            padding: 2.5rem;
            width: 100%;
            max-width: 450px;
        }

        .icon {
            text-align: center;
            font-size: 4rem;
            margin-bottom: 1rem;
        }

        h1 {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--gray-900);
            text-align: center;
            margin-bottom: 0.5rem;
        }

        .subtitle {
            text-align: center;
            color: var(--gray-500);
            margin-bottom: 2rem;
        }

        .warning {
            background: #fef3c7;
            border: 1px solid #fcd34d;
            border-radius: 0.5rem;
            padding: 1rem;
            margin-bottom: 1.5rem;
        }

        .warning h3 {
            font-size: 0.875rem;
            font-weight: 600;
            color: #92400e;
            margin-bottom: 0.5rem;
        }

        .warning p {
            font-size: 0.875rem;
            color: #78350f;
        }

        .backup-codes {
            background: var(--gray-100);
            border-radius: 0.5rem;
            padding: 1rem;
            margin-bottom: 1.5rem;
        }

        .backup-codes h3 {
            font-size: 0.875rem;
            font-weight: 600;
            color: var(--gray-700);
            margin-bottom: 0.75rem;
        }

        .codes-grid {
            display: grid;
            grid-template-columns: repeat(2, 1fr);
            gap: 0.5rem;
        }

        .code {
            font-family: monospace;
            font-size: 0.875rem;
            background: white;
            padding: 0.5rem;
            border-radius: 0.25rem;
            text-align: center;
        }

        .btn {
            display: block;
            width: 100%;
            padding: 0.875rem 1rem;
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 0.5rem;
            font-size: 1rem;
            font-weight: 500;
            cursor: pointer;
            text-align: center;
            text-decoration: none;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="icon">✅</div>
        <h1>Two-Factor Authentication Enabled</h1>
        <p class="subtitle">Your account is now more secure</p>

        <div class="warning">
            <h3>⚠️ Save Your Backup Codes</h3>
            <p>These codes can be used to access your account if you lose your authenticator. Store them in a safe
                place.</p>
        </div>

        <div class="backup-codes">
            <h3>Backup Codes</h3>
            <div class="codes-grid">
                <% backupCodes.forEach(function(code) { %>
                    <div class="code">
                        <%= code %>
                    </div>
                    <% }); %>
            </div>
        </div>

        <a href="/" class="btn">Continue to Dashboard</a>
    </div>
</body>

</html>