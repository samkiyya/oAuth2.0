<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Authorize - OAuth 2.0 Authorization Server</title>
    <style>
        :root {
            --primary: #4f46e5;
            --primary-dark: #4338ca;
            --error: #ef4444;
            --success: #10b981;
            --gray-50: #f9fafb;
            --gray-100: #f3f4f6;
            --gray-200: #e5e7eb;
            --gray-300: #d1d5db;
            --gray-500: #6b7280;
            --gray-700: #374151;
            --gray-900: #111827;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 1rem;
        }

        .container {
            background: white;
            border-radius: 1rem;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
            padding: 2.5rem;
            width: 100%;
            max-width: 480px;
        }

        .app-info {
            text-align: center;
            margin-bottom: 2rem;
        }

        .app-logo {
            width: 80px;
            height: 80px;
            background: var(--gray-100);
            border-radius: 1rem;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1rem;
            color: var(--gray-500);
        }

        .app-logo img {
            width: 60px;
            height: 60px;
            object-fit: contain;
        }

        .app-name {
            font-size: 1.25rem;
            font-weight: 600;
            color: var(--gray-900);
            margin-bottom: 0.5rem;
        }

        .app-request {
            color: var(--gray-500);
            font-size: 0.9rem;
        }

        .user-info {
            background: var(--gray-50);
            border-radius: 0.5rem;
            padding: 1rem;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 1rem;
        }

        .user-avatar {
            width: 40px;
            height: 40px;
            background: var(--primary);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 600;
        }

        .user-details {
            flex: 1;
        }

        .user-name {
            font-weight: 500;
            color: var(--gray-900);
        }

        .user-email {
            font-size: 0.875rem;
            color: var(--gray-500);
        }

        .switch-link {
            color: var(--primary);
            text-decoration: none;
            font-size: 0.875rem;
        }

        .permissions h2 {
            font-size: 0.875rem;
            font-weight: 600;
            color: var(--gray-700);
            margin-bottom: 1rem;
            text-transform: uppercase;
            letter-spacing: 0.05em;
        }

        .permission-list {
            list-style: none;
            margin-bottom: 1.5rem;
        }

        .permission-item {
            display: flex;
            align-items: flex-start;
            gap: 0.75rem;
            padding: 0.75rem 0;
            border-bottom: 1px solid var(--gray-100);
        }

        .permission-item:last-child {
            border-bottom: none;
        }

        .permission-icon {
            width: 20px;
            height: 20px;
            color: var(--success);
            flex-shrink: 0;
            margin-top: 2px;
        }

        .permission-text {
            color: var(--gray-700);
            font-size: 0.9rem;
        }

        .warning {
            background: #fef3c7;
            border: 1px solid #fcd34d;
            border-radius: 0.5rem;
            padding: 1rem;
            margin-bottom: 1.5rem;
            font-size: 0.875rem;
            color: #92400e;
        }

        .button-group {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
        }

        .btn {
            padding: 0.875rem 1rem;
            border: none;
            border-radius: 0.5rem;
            font-size: 1rem;
            font-weight: 500;
            cursor: pointer;
            transition: background-color 0.2s;
        }

        .btn-primary {
            background: var(--primary);
            color: white;
        }

        .btn-primary:hover {
            background: var(--primary-dark);
        }

        .btn-secondary {
            background: var(--gray-100);
            color: var(--gray-700);
        }

        .btn-secondary:hover {
            background: var(--gray-200);
        }

        .links {
            margin-top: 1.5rem;
            text-align: center;
            font-size: 0.75rem;
            color: var(--gray-500);
        }

        .links a {
            color: var(--gray-500);
            text-decoration: none;
        }

        .links a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="app-info">
            <div class="app-logo">
                <% if (client.logoUri) { %>
                    <img src="<%= client.logoUri %>" alt="<%= client.name %>">
                    <% } else { %>
                        <svg xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor"
                            width="40" height="40">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z" />
                        </svg>
                        <% } %>
            </div>
            <h1 class="app-name">
                <%= client.name %>
            </h1>
            <p class="app-request">wants to access your account</p>
        </div>

        <div class="user-info">
            <div class="user-avatar">
                <%= user.name ? user.name.charAt(0).toUpperCase() : user.email.charAt(0).toUpperCase() %>
            </div>
            <div class="user-details">
                <div class="user-name">
                    <%= user.name || 'User' %>
                </div>
                <div class="user-email">
                    <%= user.email %>
                </div>
            </div>
            <a href="/logout" class="switch-link">Switch</a>
        </div>

        <div class="permissions">
            <h2>This will allow <%= client.name %> to:</h2>
            <ul class="permission-list">
                <% scopes.forEach(function(scope) { %>
                    <li class="permission-item">
                        <svg class="permission-icon" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24"
                            stroke="currentColor">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7" />
                        </svg>
                        <span class="permission-text">
                            <%= scope.description %>
                        </span>
                    </li>
                    <% }); %>
            </ul>
        </div>

        <div class="warning">
            Make sure you trust <strong>
                <%= client.name %>
            </strong>. You may be sharing sensitive information.
        </div>

        <form method="POST" action="/authorize">
            <input type="hidden" name="clientId" value="<%= params.clientId %>">
            <input type="hidden" name="redirectUri" value="<%= params.redirectUri %>">
            <input type="hidden" name="scope" value="<%= params.scope %>">
            <input type="hidden" name="state" value="<%= params.state || '' %>">
            <input type="hidden" name="codeChallenge" value="<%= params.codeChallenge || '' %>">
            <input type="hidden" name="codeChallengeMethod" value="<%= params.codeChallengeMethod || '' %>">
            <input type="hidden" name="nonce" value="<%= params.nonce || '' %>">

            <div class="button-group">
                <button type="submit" name="action" value="deny" class="btn btn-secondary">Deny</button>
                <button type="submit" name="action" value="allow" class="btn btn-primary">Allow</button>
            </div>
        </form>

        <div class="links">
            <% if (client.policyUri) { %>
                <a href="<%= client.policyUri %>" target="_blank">Privacy Policy</a>
                <% } %>
                    <% if (client.policyUri && client.tosUri) { %> • <% } %>
                            <% if (client.tosUri) { %>
                                <a href="<%= client.tosUri %>" target="_blank">Terms of Service</a>
                                <% } %>
        </div>
    </div>
</body>

</html>