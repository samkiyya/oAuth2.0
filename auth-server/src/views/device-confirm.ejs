<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Confirm Device Authorization</title>
    <style>
        :root {
            --primary: #4f46e5;
            --primary-dark: #4338ca;
            --success: #10b981;
            --gray-50: #f9fafb;
            --gray-100: #f3f4f6;
            --gray-200: #e5e7eb;
            --gray-500: #6b7280;
            --gray-700: #374151;
            --gray-900: #111827;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 1rem;
        }

        .container {
            background: white;
            border-radius: 1rem;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
            padding: 2.5rem;
            width: 100%;
            max-width: 450px;
        }

        .header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .app-logo {
            width: 80px;
            height: 80px;
            background: var(--gray-100);
            border-radius: 1rem;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 1rem;
            font-size: 2.5rem;
        }

        h1 {
            font-size: 1.25rem;
            font-weight: 700;
            color: var(--gray-900);
        }

        .subtitle {
            color: var(--gray-500);
            font-size: 0.9rem;
            margin-top: 0.5rem;
        }

        .permissions {
            background: var(--gray-50);
            border-radius: 0.5rem;
            padding: 1rem;
            margin-bottom: 1.5rem;
        }

        .permissions h3 {
            font-size: 0.875rem;
            font-weight: 600;
            color: var(--gray-700);
            margin-bottom: 0.75rem;
        }

        .permission-list {
            list-style: none;
        }

        .permission-item {
            display: flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.5rem 0;
            font-size: 0.875rem;
            color: var(--gray-700);
        }

        .permission-icon {
            color: var(--success);
        }

        .warning {
            background: #fef3c7;
            border: 1px solid #fcd34d;
            border-radius: 0.5rem;
            padding: 1rem;
            margin-bottom: 1.5rem;
            font-size: 0.875rem;
            color: #92400e;
        }

        .button-group {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1rem;
        }

        .btn {
            padding: 0.875rem 1rem;
            border: none;
            border-radius: 0.5rem;
            font-size: 1rem;
            font-weight: 500;
            cursor: pointer;
        }

        .btn-primary {
            background: var(--primary);
            color: white;
        }

        .btn-primary:hover {
            background: var(--primary-dark);
        }

        .btn-secondary {
            background: var(--gray-100);
            color: var(--gray-700);
        }

        .btn-secondary:hover {
            background: var(--gray-200);
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <div class="app-logo">
                <% if (client.logoUri) { %>
                    <img src="<%= client.logoUri %>" alt="<%= client.name %>"
                        style="max-width: 100%; max-height: 100%;">
                    <% } else { %>
                        📺
                        <% } %>
            </div>
            <h1>
                <%= client.name %>
            </h1>
            <p class="subtitle">wants to access your account on this device</p>
        </div>

        <div class="permissions">
            <h3>This will allow access to:</h3>
            <ul class="permission-list">
                <% scope.forEach(function(s) { %>
                    <li class="permission-item">
                        <span class="permission-icon">✓</span>
                        <span>
                            <%= s==='openid' ? 'Your identity' : s==='profile' ? 'Your profile information' :
                                s==='email' ? 'Your email address' : s %>
                        </span>
                    </li>
                    <% }); %>
            </ul>
        </div>

        <div class="warning">
            ⚠️ Make sure you initiated this request from your device
        </div>

        <form method="POST" action="/device/authorize">
            <input type="hidden" name="user_code" value="<%= userCode %>">
            <div class="button-group">
                <button type="submit" name="action" value="deny" class="btn btn-secondary">Deny</button>
                <button type="submit" name="action" value="allow" class="btn btn-primary">Allow</button>
            </div>
        </form>
    </div>
</body>

</html>