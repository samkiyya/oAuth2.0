<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Device Authorization</title>
    <style>
        :root {
            --primary: #4f46e5;
            --primary-dark: #4338ca;
            --error: #ef4444;
            --gray-50: #f9fafb;
            --gray-200: #e5e7eb;
            --gray-500: #6b7280;
            --gray-700: #374151;
            --gray-900: #111827;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 1rem;
        }

        .container {
            background: white;
            border-radius: 1rem;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
            padding: 2.5rem;
            width: 100%;
            max-width: 400px;
            text-align: center;
        }

        .icon {
            font-size: 4rem;
            margin-bottom: 1rem;
        }

        h1 {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--gray-900);
            margin-bottom: 0.5rem;
        }

        .subtitle {
            color: var(--gray-500);
            margin-bottom: 2rem;
        }

        .error-message {
            background: #fef2f2;
            border: 1px solid #fecaca;
            color: var(--error);
            padding: 0.75rem 1rem;
            border-radius: 0.5rem;
            margin-bottom: 1.5rem;
            font-size: 0.875rem;
            text-align: left;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        label {
            display: block;
            font-size: 0.875rem;
            font-weight: 500;
            color: var(--gray-700);
            margin-bottom: 0.5rem;
            text-align: left;
        }

        input[type="text"] {
            width: 100%;
            padding: 1rem;
            border: 2px solid var(--gray-200);
            border-radius: 0.5rem;
            font-size: 1.25rem;
            text-align: center;
            letter-spacing: 0.25rem;
            text-transform: uppercase;
        }

        input:focus {
            outline: none;
            border-color: var(--primary);
        }

        .btn {
            width: 100%;
            padding: 0.875rem 1rem;
            background: var(--primary);
            color: white;
            border: none;
            border-radius: 0.5rem;
            font-size: 1rem;
            font-weight: 500;
            cursor: pointer;
        }

        .btn:hover {
            background: var(--primary-dark);
        }

        .help-text {
            margin-top: 1.5rem;
            font-size: 0.875rem;
            color: var(--gray-500);
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="icon">📺</div>
        <h1>Device Authorization</h1>
        <p class="subtitle">Enter the code shown on your device</p>

        <% if (typeof error !=='undefined' && error) { %>
            <div class="error-message">
                <%= error %>
            </div>
            <% } %>

                <form method="POST" action="/device/verify">
                    <div class="form-group">
                        <label for="user_code">Device Code</label>
                        <input type="text" id="user_code" name="user_code" value="<%= userCode %>" maxlength="9"
                            pattern="[A-Za-z0-9]{4}-?[A-Za-z0-9]{4}" required autofocus autocomplete="off"
                            placeholder="XXXX-XXXX">
                    </div>
                    <button type="submit" class="btn">Continue</button>
                </form>

                <p class="help-text">The code should look like ABCD-1234</p>
    </div>
</body>

</html>